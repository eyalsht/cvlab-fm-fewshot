# local tooling
/CLAUDE.md
.claude/
.cursor/

# planning workspace (separate private repository mounted here)
/docs/

# curated public repository, also a separate repository mounted here
/public/

# course material and local media
/references/
/vid2*
*.mp4
*.wav

# data and experiment artifacts (repo root only; src packages are unaffected).
# No trailing slash on the run stores: both are symlinks into the private docs
# repo, which holds the canonical copy so runs survive a machine, and git does
# not match a symlink against a directory-only pattern.
/data/
/results
/ablations

# python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
*.egg-info/

# local environment
.env
